// Re-export all types from types/index.ts for backward compatibility
export type {
  Rarity,
  WheelCategory,
  WheelTheme,
  Dish,
  CartItem,
  User,
  ViewState,
  CommonProps,
  ViewProps,
} from './types/index';
